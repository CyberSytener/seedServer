# All dependencies are declared in pyproject.toml.
# This file installs the package in editable mode so that
# `pip install -r requirements.txt` works out of the box.
#
# For local development (includes linting, testing, security tools):
#   pip install -e ".[dev]"
#
# For Docker builds (pinned transitive deps):
#   pip install -r requirements.lock && pip install --no-deps .
-e .

