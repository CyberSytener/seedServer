"""Pure deterministic policies for the Intent-to-Outcome Candidate surface.

This namespace must not depend on FastAPI, Redis, concrete databases, provider
clients, Saga wiring, or UI code. Lifecycle and scoring policies are introduced
by later ITO tasks.
"""

__all__: tuple[str, ...] = ()
